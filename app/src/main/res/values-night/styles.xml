<resources>

    <style name="DisTheme" parent="Theme.AppCompat.NoActionBar"> <!-- Change Light.NoActionBar to NoActionBar -->
        <item name="colorPrimary">@color/bg_primary_blue_dark</item>
        <item name="colorPrimaryDark">@color/bg_primary_blue</item>
        <item name="colorAccent">@color/bg_primary_blue</item>
        <item name="android:navigationBarColor">@color/bg_primary_blue_dark</item> <!-- Uncomment for dark navigation bar -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="toolbarStyle">@style/Widget.MyApp.ActionBar</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:statusBarColor">@color/bg_primary_blue_dark</item> <!-- Set dark color for status bar -->
    </style>

    <style name="DisTheme.Settings" parent="Theme.AppCompat.NoActionBar"> <!-- Change Light.NoActionBar to NoActionBar -->
        <item name="android:paddingStart">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingEnd">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
    </style>

    <style name="Widget.MyApp.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="theme">@style/ThemeOverlay.MyApp.ActionBar</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Dark</item> <!-- Use dark popup theme -->
    </style>

    <style name="ThemeOverlay.MyApp.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:actionMenuTextColor">@color/white</item> <!-- Change to white for contrast -->
    </style>

    <style name="DisTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColor">@color/white</item> <!-- Ensure text is white in overlays -->
    </style>

    <style name="DisTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">@color/bg_primary_blue</item>
    </style>

</resources>
